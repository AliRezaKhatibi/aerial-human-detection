Aerial Human Detection - Git Manager GUI V2.4 Portable

FIXES IN V2.4
- Blank Git output lines no longer cause: Cannot bind argument to parameter Message.
- Temporary local changes are restored with stash apply.
- The stash is dropped only after a successful restore.
- When a same-name file arrives from GitHub, the local backup remains safely in stash@{0}.
- Restore failures now show the real Git message instead of a misleading PowerShell binding error.

RUN
Double-click Launch-Git-Manager-GUI-V2.4.vbs

Keep all package files together after extraction.
